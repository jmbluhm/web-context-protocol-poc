# Base URL for the API (used in instruction generation)
# For development: http://localhost:3000
# For production: https://your-domain.vercel.app
BASE_URL=http://localhost:3000

# Forwarding URL for cancel and change requests (optional)
# When set, requests will be forwarded to this URL with /cancel or /change paths
# Example: https://eoodwrpeul173g0.m.pipedream.net
# Cancel requests go to: {FORWARDING_URL}/cancel?token={TOKEN}
# Change requests go to: {FORWARDING_URL}/change?token={TOKEN}
FORWARDING_URL=https://eoodwrpeul173g0.m.pipedream.net

# Optional: Enable debug logging
# DEBUG=true

# Optional: Add your production domain for Vercel deployment
# PRODUCTION_URL=https://your-domain.vercel.app 